use dummy_rust_project::hello;

fn main() {
    let unused_var: i32;
    let say_hello = hello("Stackie");
    println!("{}", say_hello);
    let vec: Vec<isize> = Vec::new();
    if vec.len() <= 0 {}
    if 100 > i32::MAX {}
}
